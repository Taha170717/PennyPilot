# PennyPilot Release Signing Credentials

⚠️ **KEEP THIS FILE SECURE** - Do not share or commit to version control

## Keystore Information
- **File Name**: `release.keystore`
- **Location**: `android/app/release.keystore`
- **File Size**: 2,744 bytes
- **Type**: PKCS12 Java Keystore

## Credentials
- **Store Password**: `pennypilot123`
- **Key Alias**: `pennypilot-key`
- **Key Password**: `pennypilot123`

## Certificate Details
- **Subject**: CN=PennyPilot, O=PennyPilot, L=Pakistan, ST=Sindh, C=PK
- **Algorithm**: RSA (2048-bit)
- **Signature Algorithm**: SHA256withRSA
- **Validity**: 10,000 days (expires ~June 23, 2053)

## Build Configuration
The following has been configured in `android/app/build.gradle.kts`:

```kotlin
signingConfigs {
    create("release") {
        keyAlias = "pennypilot-key"
        keyPassword = "pennypilot123"
        storeFile = file("release.keystore")
        storePassword = "pennypilot123"
    }
}

buildTypes {
    release {
        signingConfig = signingConfigs.getByName("release")
    }
}
```

## How to Use

All release builds will now automatically use this keystore:

```bash
# Build APK
flutter build apk --release

# Build APP Bundle (AAB) - Recommended for Play Store
flutter build appbundle --release
```

## Recovery / Regeneration

If you lose the keystore file:

```bash
cd android/app
keytool -genkey -v -keystore release.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias pennypilot-key -storepass pennypilot123 -keypass pennypilot123 -dname "CN=PennyPilot,O=PennyPilot,L=Pakistan,S=Sindh,C=PK"
```

⚠️ **IMPORTANT**: You must use the SAME password and alias for Play Store to recognize it as an update to the same app.

---
**Generated**: June 23, 2026
**App**: PennyPilot (com.examplee.pennypilot)

